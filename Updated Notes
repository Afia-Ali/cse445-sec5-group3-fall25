The dataset used is from Dataset.v1i.yolov8

The results is in Turn_Prediction/runs/detect/train
Because the files are big, I simple was able to upload the results and skip the dataset cause it is already uploaded.
